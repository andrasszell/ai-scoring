"""Inference layer (Team 2).

Consumes the evidence corpus produced by the collection layer and turns it into an
AI Depth Score. This is intentionally a separate package: the collection team does
NOT score companies. The current scorer is the MVP heuristic carried over from the
prototype and is expected to be replaced by the mathematical inference model.
"""

__version__ = "0.2.0"
